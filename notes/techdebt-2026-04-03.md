# Tech Debt Report — 2026-04-03

## Metrics Summary

| Metric | Count |
|--------|-------|
| TODO/FIXME markers | 1 (benign — CLAUDE.md reference to TODO.md) |
| Broken cross-references | 2 |
| KB files missing `source:` metadata | 22 / 27 (81%) |
| KB files missing frontmatter entirely | 2 |
| Agents missing `## Verification` section | 2 / 4 (50%) |
| INDEX.md file_count mismatch | YES (says 30, actual 27) |
| INDEX.md category count mismatches | 3 categories |
| Duplicate content files | 1 (product-design-cycle.md) |
| Missing infrastructure | 2 (notes/, .claude/settings.json) |
| Missing settings.json (project-level) | YES |

---

## P0 — Structural Integrity Issues

### S1. `knowledge/INDEX.md` file_count is wrong

**Current**: `file_count: 30` | **Actual**: 27 non-INDEX .md files in knowledge/
**Category mismatches**:
- `overview/`: INDEX says 2, actual 3 (missing `design-principles.md` entry)
- `reference/`: INDEX says 2, actual 1 (only `product-design-cycle.md` exists)
- `schemas/`: INDEX says 2, actual 3 (missing `cc-architecture-diagrams.md` entry)

**Impact**: Misleading metadata degrades trust in INDEX as navigation tool. Model may search for nonexistent files or miss real ones.

### S2. Two broken internal cross-references

| Source File | Broken Link | Issue |
|-------------|-------------|-------|
| `docs/claude_code_architecture_guide_human-facing.md` | `custom_llm_macro_system_architecture.md` | File doesn't exist anywhere in repo |
| `knowledge/components/agent-skills.md` | `reference.md` | Ambiguous target — no `reference.md` at same level |

### S3. Missing project-level `.claude/settings.json`

CAB has `.claude/settings.local.json` (permissions only) but no `settings.json`. The CAB CLAUDE.md and templates reference orchestrator-as-default-agent, hooks, and model settings — but CAB itself doesn't configure these. **CAB doesn't eat its own dog food.**

### S4. Duplicate `product-design-cycle.md`

Identical file (18,575 bytes, byte-for-byte match) exists at:
- `knowledge/reference/product-design-cycle.md`
- `docs/_internal/product-design-a-team/product-design-cycle.md`

`docs/_internal/` is gitignored. The knowledge/ copy is the canonical one. The internal copy is dead weight that can drift silently.

---

## P1 — Schema & Metadata Compliance

### M1. 81% of KB files missing `source:` frontmatter field

Only 5 of 27 KB files have `source:` metadata linking to official docs:
- `components/agent-skills.md` ✓
- `components/mcp-integration.md` ✓
- `components/memory-claudemd.md` ✓
- `components/subagents.md` ✓
- `operational-patterns/orchestration-framework.md` ✓

**22 files lack provenance tracking.** Per CAB's own architecture philosophy and CLAUDE.md constraint ("check static files align with latest official Claude Code docs"), `source:` URLs are essential for freshness verification.

### M2. Two KB files have NO frontmatter at all

- `knowledge/reference/product-design-cycle.md` — no `---` block
- `knowledge/schemas/cc-architecture-diagrams.md` — no `---` block

These files are invisible to any metadata-based discovery or validation tooling.

### M3. 50% of agents missing `## Verification` section

Per CAB Tenet 1 ("Verification as Architectural Requirement"), every agent MUST have a Verification section:
- `agents/architecture-advisor.md` — **MISSING**
- `agents/project-integrator.md` — **MISSING**
- `agents/orchestrator.md` — has it ✓
- `agents/verifier.md` — has it ✓

### M4. Agent frontmatter field inconsistency

- `architecture-advisor.md` — missing `permissionMode:`
- All agents use `model: opus` except `verifier.md` which uses `model: inherit`
- No standardized model policy documented

---

## P2 — State Management & Self-Governance

### G1. `notes/` directory did not exist

CLAUDE.md says "Use `notes/` directory for session scratch work and context persistence" but the directory didn't exist. Created during this scan.

### G2. TODO.md is the sole state tracking mechanism

No `notes/progress.md`, `notes/current-task.md`, or structured state files exist. TODO.md is a monolithic 14 KB file mixing completed work (P0, P0.5 — all done), active items (P-LIVE), and deferred items (P2-P4). The completed sections are 60%+ of the file weight.

### G3. CAB doesn't use its own orchestrator-as-default pattern

`settings.json` template prescribes `"agent": "orchestrator"` but CAB's own project has no `settings.json`. The project can't demonstrate or test the pattern it teaches.

### G4. No sync protocol documented

P1-HOT reconciliation (just completed) revealed systematic drift between global and CAB. No document in `knowledge/` or `CLAUDE.md` establishes the upstream/downstream convention or sync procedure.

---

## P3 — Content Freshness & Accuracy

### F1. Templates may not reflect latest CC docs

Per CLAUDE.md constraint: "You must frequently check static files align with latest official Claude Code docs." Key files needing freshness check:
- `templates/plugin/settings.json.template` — references `hooks.PostToolUse` pattern, `permissions.allow` format
- `templates/plugin/plugin.json.template` — schema may have evolved
- `templates/global/CLAUDE.md.template` — structure recommendations
- `knowledge/components/hooks.md` — hook event types and matcher syntax
- `knowledge/components/subagents.md` — agent frontmatter fields, tool list options
- `knowledge/schemas/global-user-config.md` — directory structure
- `knowledge/schemas/distributable-plugin.md` — plugin schema

### F2. Human-facing guide broken reference

`docs/claude_code_architecture_guide_human-facing.md` references `custom_llm_macro_system_architecture.md` which doesn't exist. Guide may have other stale content not surfaced by this scan.

### F3. `docs/v1/` contains large binaries (.mp4, .pdf, .pptx)

These are gitignored but present on disk. The `.html` file dates to Mar 3, the others to Mar 5-11. These may be superseded by current documentation and could be archived or hosted externally.

---

## P4 — Architectural Consistency

### A1. `settings.local.json` has ad-hoc permissions

```json
"allow": [
  "Bash(while read:*)",
  "Bash(do echo:*)",
  "Bash(done)",
  "Bash(xargs grep:*)"
]
```
These are debugging artifacts, not intentional architecture. They bypass the principle of least privilege.

### A2. Placeholder tokens in non-template files

`{{PLACEHOLDER}}` syntax correctly used in `templates/` and `skills/creating-components/` (scaffolding skill), but `skills/scaffolding-projects/SKILL.md` also uses them in procedural code blocks. This is expected behavior (the skill generates files with placeholders), but should be documented to avoid confusion during automated scans.

### A3. INDEX.md files exist for all 9 knowledge categories ✓

This is a positive finding — every category has an INDEX.md.

---

## Auto-fixable Items

| # | Item | File | Fix |
|---|------|------|-----|
| 1 | INDEX file_count | `knowledge/INDEX.md` | Change 30 → 27 |
| 2 | INDEX overview count | `knowledge/INDEX.md` | Change 2 → 3, add `design-principles.md` |
| 3 | INDEX reference count | `knowledge/INDEX.md` | Change 2 → 1 |
| 4 | INDEX schemas count | `knowledge/INDEX.md` | Change 2 → 3, add `cc-architecture-diagrams.md` |
| 5 | Duplicate product-design-cycle | `docs/_internal/` copy | Delete (gitignored, canonical is in knowledge/) |
| 6 | settings.local.json cleanup | `.claude/settings.local.json` | Remove ad-hoc debug permissions |

## Manual Review Required

| # | Item | Scope | Suggested Approach |
|---|------|-------|-------------------|
| 7 | Add `source:` to 22 KB files | 22 files | Batch update with official CC doc URLs from https://docs.anthropic.com/en/docs/ |
| 8 | Add frontmatter to 2 KB files | 2 files | Add type/title/source/related fields |
| 9 | Add Verification sections to 2 agents | 2 files | Follow verifier.md as pattern |
| 10 | Create `.claude/settings.json` | new file | Use settings.json.template as base, customize for CAB |
| 11 | Fix 2 broken cross-references | 2 files | Identify correct targets or remove links |
| 12 | Restructure TODO.md | 1 file | Archive completed sections, keep active + deferred only |
| 13 | Document sync protocol | new KB file | CAB = upstream, global = downstream, procedure + hook |
| 14 | Freshness audit against CC docs | ~10 files | Use claude-docs-helper skill to verify accuracy |
| 15 | Standardize agent model policy | 4 files + docs | Document when to use opus vs inherit vs sonnet |

## Tracked (Known Debt from TODO.md)

- P1-HOT reconciliation items 1-6: **RESOLVED this session**
- P1-HOT full audit + sync protocol: **PENDING** (deferred to interactive planning)
- P2 template population: deferred to post-P-LIVE
- P3 plugin audit: deferred
- P4 RAG evaluation: deferred
