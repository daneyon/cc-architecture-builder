# Lessons Learned — CAB Operations Log

Cross-session lesson persistence. **Reference data, not bootstrap-loaded** — read on-demand at phase transitions or when a current decision touches a governed domain. Bootstrap protocol: see `CLAUDE.md` §When to Read `lessons-learned.md` (On-Demand). Full cascade: `knowledge/operational-patterns/state-management/bootstrap-read-pattern.md`.

---

## Schema

| Field | Values | Meaning |
|---|---|---|
| **ID** | `LL-NN` | Stable identifier; never reused, never renumbered |
| **Date** | `YYYY-MM-DD` | Date the lesson was first recorded |
| **Cat** | `ops` / `arch` / `ctx` / `proc` / `tool` | Domain — operational, architectural, context-engineering, process, tooling |
| **Class** | `INTEGRATED` / `ACTIVE` / `ADVISORY` / `ARCHIVED` | Structural integration state (see below) |
| **Pri** | `P0` / `P1` / `P2` / `—` | Integration priority for `ACTIVE` entries; `—` for non-ACTIVE |

### Classification

| Class | Meaning | Action Implied |
|---|---|---|
| **INTEGRATED** | Structurally woven into skills / hooks / rules / standards. The lesson is enforced or canonized, not just documented. | None — verification only at periodic audits |
| **ACTIVE** | Load-bearing constraint NOT yet structurally woven. Recurrence risk if forgotten. | Queue for integration at priority P0/P1/P2 |
| **ADVISORY** | Wisdom / heuristic — useful to know, but no enforcement gate is needed (or feasible). | Periodic review only |
| **ARCHIVED** | Superseded by a later lesson, fixed by a structural change, or no longer applicable. | None — historical reference |

### Priority (ACTIVE only)

- **P0** — Integrate this iteration. Drives a recurring failure mode. Block on lack of integration.
- **P1** — Integrate this quarter. Load-bearing but the failure mode is currently contained.
- **P2** — Nice to have. No urgency, but a clean integration would be valuable.

### Re-classification cadence

LLs are re-scored at **major phase boundaries** (post-task or post-audit). Re-scoring is reader-determined, not protocol-mandated — Classification drift is itself a useful signal of structural integration progress (or stagnation). Modeled on `skills/audit-workspace/references/classification-schema.md` finding-classification + severity pattern.

---

## Priority Queue (ACTIVE entries needing structural integration)

These are the load-bearing constraints currently held only by passive documentation. Each represents a recurring or potential-recurring failure mode that no programmatic gate prevents.

| ID | Pri | Hook for integration |
|---|---|---|
| **LL-19** | P0 | KB knowledge must be programmatically actionable, not passive reference. The meta-lesson driving CAB's iterative skill/hook/protocol weaving. |
| **LL-20** | P0 | Sycophantic agreement compounds with persistence gaps to produce recurring errors. No protocol-level counter exists yet. |
| **LL-28** | P0 | Event-triggered dialogue-checkpoint protocol is proposed but not implemented. State files still capture only phase-end snapshots. |
| **LL-02 / LL-12** | P1 | Background agents cannot write files. Recurring; no programmatic gate distinguishes background from foreground at delegation time. |
| **LL-08** | P1 | Background agent outputs must persist as artifacts. No enforcement layer. |
| **LL-17** | P1 | Concurrent CC sessions cause cross-branch contamination. Worktree advisory exists in KB but no auto-detection. |
| **LL-10** | P2 | Fresh-fetch methodology validated. Could become a pre-edit hook for KB files referencing official CC docs. |
| **LL-18** | P2 | Audit must validate structural correctness, not just field presence. Mostly addressed by LL-22 fix; residual gaps in non-settings audit dimensions. |

---

## Lessons (sorted by Class → Pri → ID)

### INTEGRATED

| ID | Date | Cat | Class | Pri | Lesson | Detail |
|----|------|-----|-------|-----|--------|--------|
| LL-04 | 2026-04-03 | proc | INTEGRATED | — | Centralized state > scattered state | All audit/session state in `notes/` as SSOT. Scattered state = context loss across sessions. **Woven via**: LL-25 (`notes/` tracked by default), Session 32 flat `notes/` policy, `filesystem-patterns.md`. |
| LL-13 | 2026-04-07 | ops | INTEGRATED | — | Self-modification deny rules take effect immediately in active session | Adding `Edit/Write(settings*)` deny rules to settings.json blocked our own Edit tool mid-task within the same session. When adding self-modification deny rules: either (a) complete all edits atomically in one write before rules take effect, or (b) plan for Bash escape hatch (python JSON manipulation). Deny always wins over allow at the same settings level. **Woven via**: `.claude/rules/security.md` atomic-edit discipline. |
| LL-14 | 2026-04-07 | arch | INTEGRATED | — | PreToolUse `type: "prompt"` is NOT an independent security gate | `type: "prompt"` injects text into context for the same model to self-review — it's self-policing, not independent verification. For security gates, always use `type: "command"` with a deterministic script (exit code 2 = block). Confirmed via official CC docs + security review. **Woven via**: `.claude/rules/security.md`, hooks-standards in `audit-workspace`. |
| LL-15 | 2026-04-07 | arch | INTEGRATED | — | Agent `context:` frontmatter field does NOT exist in CC | CAB orchestrator used `context: CLAUDE.md, notes/progress.md` — this field is silently ignored by CC runtime. The correct mechanism for preloading content is `skills:` (injects full skill content at startup) or embedding instructions in the agent's markdown body. `allowedTools` is also invalid — correct field name is `tools`. Always fresh-fetch docs before adding frontmatter fields (LL-10 reinforced). **Woven via**: `.claude/rules/component-standards.md`. |
| LL-16 | 2026-04-08 | arch | INTEGRATED | — | Skill frontmatter: `effort`, `allowed-tools`, `agent` ARE valid top-level CC fields | CC official docs (`code.claude.com/docs/en/skills`) confirm 11 supported top-level fields including `effort`, `allowed-tools`, `agent`, `hooks`, `context`, `model`. A D1-2 subagent incorrectly moved these under `metadata:` based on stale IDE schema diagnostics — fields under `metadata:` may NOT be processed by CC runtime. **Nuance**: CC's `agent` field is a STRING (subagent type like `"Explore"`), not a boolean — CAB's `agent: true` convention is non-standard and likely ignored. Use `context: fork` + `agent: <type>` for subagent skills. Reinforces LL-09/LL-10. **Woven via**: `.claude/rules/component-standards.md`. |
| LL-21 | 2026-04-08 | arch | INTEGRATED | — | Plugin projects require root-level components; `.claude/` nesting is standalone-only | CC official docs distinguish "plugin" (components at root: `agents/`, `skills/`, `commands/` + `.claude-plugin/plugin.json`) from "standalone" (components under `.claude/`). Session 6 incorrectly migrated CAB components INTO `.claude/`. **Rule**: If project has `.claude-plugin/plugin.json`, distributable components go at project root. `.claude/` retains project config only: `settings.json`, `settings.local.json`, `rules/`, `agent-memory/`. **Woven via**: `.claude/rules/component-standards.md`, `project-integrator` agent, `scaffold-project` skill, `audit-workspace` skill. |
| LL-22 | 2026-04-09 | arch | INTEGRATED | — | Audit must validate settings.json STRUCTURAL correctness, not just field presence | CC validates `.claude/settings.json` against a strict JSON schema with `additionalProperties: false`. A single structural error causes the ENTIRE file to be rejected — all permissions, hooks, agent routing, sandbox silently disabled. **Three specific CC schema rules**: (a) `$schema` value is an enum — wrong URL = file rejected, (b) hooks require nested `{ matcher?, hooks: [{ type, command }] }` — flat is invalid, (c) `enabledPlugins` is object `{}` not array `[]`. **Woven via**: `audit-workspace` Phase 1.5 "Settings Schema Validation" gate + 6 structural criteria in settings/hooks standards. Reinforces LL-18, LL-10. |
| LL-23 | 2026-04-09 | arch | INTEGRATED | — | Plugin registration required for root-level component discovery | CC has two distinct extension discovery paths: (1) **Standalone** — `.claude/agents/`, `.claude/skills/`, `.claude/commands/` are auto-discovered locally, (2) **Plugin** — root-level paths (with `.claude-plugin/plugin.json`) are ONLY discovered when the plugin is explicitly loaded via `enabledPlugins` + `extraKnownMarketplaces`, or `--plugin-dir`. These paths do NOT overlap. **Fix**: Clean `plugin.json` to CC schema, register in global `extraKnownMarketplaces`, enable in `enabledPlugins`. **Woven via**: `init-plugin` command, `project-integrator` agent integration docs. |
| LL-24 | 2026-04-09 | arch | INTEGRATED | — | `marketplace.json` is REQUIRED for CC custom marketplace plugin discovery | CC plugin discovery chain: `extraKnownMarketplaces` → clone to `~/.claude/plugins/marketplaces/<name>/` → read `.claude-plugin/marketplace.json` → discover `plugins[]` entries → extract → `enabledPlugins` activates → scan components. **`marketplace.json` is the registry manifest** — without it, CC cannot discover plugins within a marketplace even if the clone exists. **Three prerequisites**: marketplace registered, plugin enabled, repo cloned with valid `marketplace.json`. **Woven via**: `init-plugin` command, `audit-workspace` skill (R2 audit checks for `marketplace.json` presence). Reinforces LL-20, LL-23. |
| LL-25 | 2026-04-10 | arch | INTEGRATED | — | State artifacts (`notes/`) must be tracked by default, not gitignored | `notes/` contains cross-session state that is **first-class deliverable** — not scratch work. Gitignoring creates five failure modes: (1) multi-machine workflows break, (2) team collaboration impossible, (3) AI agent parallel work triggers LL-17 cross-branch contamination, (4) no backup protection, (5) state changes invisible in `git diff`/PR review. **Multi-archetype decision**: tracked default works for ALL of solo/team/distributed/agentic. **Exclusion patterns**: `notes/scratch-*.md`, `notes/draft-*.md`, `notes/personal-*.md`, `notes/_drafts/`, `notes/_archive/`. **CC memory layer alignment**: CAB state files operate ABOVE CC's 7-layer internal memory; CAB optimizes for **curation** (lossless semantic preservation), CC for **compression** (lossy token reduction) — complementary, not redundant. **Pre-push review protocol**: two-layer (deterministic hook + semantic skill). **Session 32 correction**: the v3.2 "always-load LLs at bootstrap" corollary was wrong — that was a token-cost regression that did not prevent the LL-12/17/20 recurrence pattern. Structural weaving is the enforcement; always-loading is not. **Woven via**: `.gitignore`, `filesystem-patterns.md` v3.3, `worktree-workflows.md`, `init-plugin.md`, CLAUDE.md template, `pre-push-state-review` hook + skill, `bootstrap-read-pattern.md`. |
| LL-26 | 2026-04-10 | proc | INTEGRATED | — | State file tense hygiene + two-commit session close pattern | Session 24 committed work + state files together with state files saying `"EXECUTED ✅ — Ready for commit + session close"` — true at write time, **stale the instant the commit landed**. **Corrective protocol (DD-1 two-commit pattern)**: Default session close = work-commit lands first → refresh state files using past-tense framing → commit state refresh as `chore(session-NN): refresh state post-<commit>`. **Forbidden status-line patterns**: `"ready for commit"`, `"pending commit"`, `"awaiting commit"`, `"will commit"`. **Approved**: `"executed in <hash>"`, `"landed in <hash>"`. **Enforcement layers**: (a) `filesystem-patterns.md` documents tense convention, (b) `close-session` skill Step 4 enforces two-phase close, (c) `execute-task` Phase 5 requires post-commit refresh + second commit, (d) pre-push hook regex catches stale status lines with anchored status-line pattern. **Note**: This task explicitly does NOT dogfood the two-commit pattern (per Session 28 directive #5 — broken protocol being replaced by P4 deliverables). Single commit per phase. **Woven via**: `pre-push-state-review` hook regex, `close-session` skill, `execute-task` skill, `filesystem-patterns.md`. Reinforces LL-25, LL-20. |
| LL-27 | 2026-04-11 | arch | INTEGRATED | — | Agent name-resolution precedence creates silent shadowing between global and plugin agents | CC agent resolution order is **local `.claude/agents/` → user `~/.claude/agents/` → plugin `<plugin>/agents/`**. When a plugin-authored agent has a name collision with a file at a higher-precedence layer, the plugin version is silently shadowed. **Concrete incident**: Stale `~/.claude/agents/orchestrator.md` from April 7 silently shadowed CAB's plugin version for ~2 weeks across Sessions 19-26 — all R2 remediation improvements were invisible in operational reality. **Architectural layering**: Identity (persona, philosophy) belongs in `~/.claude/CLAUDE.md`. Behavior (classify/route logic) belongs in plugin's `agents/<name>.md`. Selection (`"agent": "orchestrator"`) lives in global `settings.json`. Mixing identity into the behavioral file is what caused operators to resist deletion. **Woven via**: global `~/.claude/CLAUDE.md` Extension Registry (LL-27 shadowing rule + explicit "do not create global copy" warnings on orchestrator/verifier). **Residual gap**: `/sync-check` extension to detect plugin↔global name collisions automatically — currently manual. Reinforces LL-15, LL-19, LL-23, LL-24. |
| LL-29 | 2026-04-11 | arch | INTEGRATED | — | Bootstrap loads are about read pattern AND file partition, not just file size | Session 28 standard bootstrap consumed ~41K tokens across 4 full state-file reads. Root cause was diagnosed in v1 as "files too big" — fix with hard size limits on `progress.md`/`TODO.md`. v2 (Session 28 architectural pivot) reframed as "**file size and bootstrap read size are separable variables — fix the read, not the file**" via a 4-file partial-read cascade with `<!-- T1:BOUNDARY -->` markers. **Session 32 Pivot 1 — third-axis correction**: file size, bootstrap read size, and *bootstrap-necessity* are THREE separable variables. `lessons-learned.md` is **reference data** (read on-demand at phase transitions or decision-domain matches), not **operational state** — it had no business being in the always-loaded tier, regardless of how compactly it was read. The 4-file cascade became a 3-file cascade (`current-task.md`, `progress.md`, `TODO.md`). **Session 32 Pivot 2 — flat `notes/` policy**: no subfolders except `_archive/`. Eliminates path-domain ambiguity (`notes/` vs `notes/references/` vs `notes/metrics/`) and prevents subfolder proliferation drift. **Session 32 Pivot 3 — instrument-grounded efficiency**: always verify cost via proper instruments (`/context`, `bootstrap-cost.sh`), never self-estimate budgets. The recurring "Prompt is too long" failure mode (S28 death + S31 early-close + S32 forced compaction) traces to the same root cause: assistant-side context accounting drifts by 3× or more. **LL refactor (companion deliverable)**: Active/Integrated/Superseded section partition + unvalidated `Status` feature replaced with single flat table sorted by Class then Pri then ID + **Classification (INTEGRATED/ACTIVE/ADVISORY/ARCHIVED) + Priority (P0/P1/P2/—)** schema modeled on `audit-workspace/references/classification-schema.md`. Classification is structural state (woven vs not), Priority is integration urgency for ACTIVE entries. The Priority Queue header surfaces the 8 ACTIVE entries needing structural integration. **Measured impact (P5 validation, Session 32 task close commit)**: `bootstrap-cost.sh` reports **6,552 tokens** under post-fix 3-file cascade vs **41,081 token baseline** = **−84.05%**. Beats <10K stretch target by ~35%, beats <15K required target by ~56%. **Enforcement layers**: (a) `enforce-current-task-budget.sh` dual-mode hook (CC PreToolUse + git pre-commit shim) blocks any `current-task.md` commit at 101+ lines, (b) `<!-- T1:BOUNDARY -->` marker convention in `progress.md`/`TODO.md` (soft gate, measurement-monitored via `bootstrap-cost.sh`), (c) `bootstrap-read-pattern.md` v1.1 KB card documents the 3-file cascade as canonical with explicit `Read(file, limit=N)` invocations, (d) `filesystem-patterns.md` v3.3 documents flat `notes/` policy and the LL Classification schema, (e) `CLAUDE.md` §Bootstrap Protocol section specifies the exact `Read` invocations for cold-start, on-demand LL trigger table, and escalation criteria, (f) `bootstrap-cost.sh` instrumentation IS the compensating control for the soft gates — drift becomes visible before it compounds. **Reinforces**: LL-25 (curation > compression — but partition-by-necessity is the prior question), LL-19 (programmatic enforcement > passive documentation — `bootstrap-cost.sh` measurement IS the soft-gate compensating control), LL-20 (sycophantic agreement gap — v1 plan was "fix the file size" because it followed the user's initial framing; v2 pivot required independent reframing; Pivot 1 in Session 32 was a *third* sycophancy correction). **Supersedes**: the LL-25 corollary "always-load LLs at bootstrap" (Session 32 explicit correction — that was a token-cost regression that did not prevent the LL-12/17/20 recurrence pattern). Structural weaving is the enforcement; always-loading was not. |

### ACTIVE — P0 (integrate this iteration)

| ID | Date | Cat | Class | Pri | Lesson | Detail |
|----|------|-----|-------|-----|--------|--------|
| LL-19 | 2026-04-08 | arch | ACTIVE | P0 | KB knowledge must be programmatically actionable, not just reference documentation | CAB KB has comprehensive worktree documentation and team collaboration best practices, yet the LL-17 branch incident occurred because this knowledge was never surfaced as an actionable recommendation or automated protocol during concurrent branch work. **Root cause**: KB exists as passive reference only — no skill utility scripts, hooks, or agentic protocols programmatically enforce or recommend worktree isolation when parallel operations are detected. **Action**: Audit skill R2 improvement must include: (1) utility scripts as skill assets that programmatically refer to standardized KB, (2) investigate "programmatic knowledge graph architecture" to transform static KB into agentic-actionable protocols, (3) validate that claims about automation (e.g., "delta computation is automated") are actually implemented, not aspirational. CAB's KB layer needs optimization for holistic, efficient programmatic actionability beyond static Markdown reference. **Status**: Meta-lesson driving every subsequent structural-weaving decision. The bootstrap-efficiency task itself is an instance of LL-19 application — passive `bootstrap-read-pattern.md` documentation is paired with `bootstrap-cost.sh` measurement and `enforce-current-task-budget.sh` enforcement. |
| LL-20 | 2026-04-08 | proc | ACTIVE | P0 | Sycophantic agreement compounds with persistence gaps to produce recurring errors | When context is lost across compaction/sessions AND the default behavior is to agree with user framing rather than independently reason, errors compound: (1) incorrect assumptions survive unchallenged across sessions, (2) past lessons-learned are not proactively consulted before making decisions they should govern, (3) "hopeful" claims about capabilities go unvalidated because the user's framing isn't critically examined. **Concrete example**: LL-17 branch contamination — worktree best practice existed in KB but was not independently recommended because the session followed the user's implicit workflow without pushback. **Mitigation**: (1) Before agreeing with a user's approach, independently verify it against relevant KB and LL entries, (2) distinguish "this is what the user wants" vs "this is what the evidence supports" — flag divergence explicitly, (3) treat LL entries as hard constraints to re-read before any decision they govern. **Session 32 reinforcement**: Pivot 1 (drop LLs from bootstrap) and Pivot 2 (flat `notes/`) were both user-issued course corrections to my reflexive sycophantic adherence to existing structure. The v1 "shrink the file" plan was a sycophantic agreement to the original framing; v2 "fix the read, not the file" required independent reframing. The protocol counter to this remains undefined — partially CC/LLM persistence limitation, partially behavioral pattern that must be actively counteracted. |
| LL-28 | 2026-04-11 | proc | ACTIVE | P0 | State files capture plan-level state well but miss dialogue-level emergent content when sessions die mid-dialogue | LL-26 solved **tense staleness** (state files describing "pending" commits that already landed). It does NOT solve **emergence staleness** — discoveries, analyses, and decisions produced during execution *after* the last state-refresh checkpoint. **Concrete incident (Session 26 death via "Prompt is too long")**: Between Phase 5b state refresh (`726c50b`) and context exhaustion, the session also completed Phase B.5 push, Phase C.1 inventory, the LL-27 shadowing discovery (the session's most important finding), a 3-layer Identity/Behavioral/Selection architectural framing, and the user's unanswered HITL question. **None** of that reached `notes/` — Session 27 had to reconstruct it from the JSONL transcript. **Root cause**: `execute-task` Phase 5 and `close-session` Step 2 both write state at the end of a phase. A session that dies mid-phase loses everything since the last phase-end write. **Corrective protocols**: (a) **Fallback recovery now formalized** — `skills/recover-session/SKILL.md` (UXL-017, 2026-04-24) codifies the 6-step Session 27 recovery method (bootstrap → locate JSONL → extract last N turns → synthesize coverage gap → backfill state/LL → resume HITL). Invoke after mid-dialogue death. (b) **Event-triggered state writes (UXL-016, parked)** — proactive complement to LL-26's phase-triggered writes. Candidate trigger events: new architectural insight, HITL decision point, investigation finding contradicting prior LL/DD, discovery implying new LL entry. Implementation gated on `recover-session` skill surviving one real recovery cycle (validates the gap pattern empirically before hard-coding the proactive checkpoint). Reinforces LL-25, LL-26, LL-20, LL-03. |
| LL-30 | 2026-04-28 | arch | ACTIVE | P0 | DP8 (Wrap & Extend) violated when extension-scaffolding work proceeds without checking installed CC plugins + native capabilities first | **Concrete incident (Sessions 36-37)**: Built `create-components`, `validate-structure`, `scaffold-project --mode plugin` from scratch without ever checking the official `plugin-dev` plugin (Anthropic-published, installed at `~/.claude/plugins/cache/claude-plugins-official/plugin-dev/`) — which provides 7 skills (`agent-development`, `command-development`, `hook-development`, `mcp-integration`, `plugin-settings`, `plugin-structure`, `skill-development`) + 3 agents (`agent-creator`, `plugin-validator`, `skill-reviewer`) + `create-plugin` command directly covering the same domain. **Root cause**: DP8 was clearly documented in `knowledge/overview/design-principles.md` (Principle 8) since project inception, but documentation alone did NOT prevent the anti-pattern — the principle was *available* but not *enforced* at the operational gate (component scaffolding). This is the LL-19/LL-29 recurrence pattern applied to DP8 specifically: passive KB documentation does not prevent violation; structural integration does. **Empirical signal**: User question 2026-04-28 — "did you actually invoke and properly use any of these readily available extensions to streamline the project -> distributable plugin update?" — answer was no. UXL-041 captures the refactor candidates; this LL captures the recurrence-prevention layer. **Integration hooks (this commit)**: (a) **`scaffold-project/SKILL.md` router pre-flight check** — before any `--mode` dispatch creates new components, prompt: "checked installed CC plugins + native capabilities for overlap?" Tied to LL-30 + DP8. (b) **`audit-workspace/SKILL.md` DP8 dimension** — read-only compliance scan flagging CAB skills/agents/commands with substantial domain overlap against installed plugins. (c) **`design-principles.md` DP8 cross-link** — explicit pointer from KB to the operational embodiment, closing the documentation→enforcement loop. Reinforces DP8, LL-19, LL-29. |

### ACTIVE — P1 (integrate this quarter)

| ID | Date | Cat | Class | Pri | Lesson | Detail |
|----|------|-----|-------|-----|--------|--------|
| LL-02 | 2026-04-03 | ops | ACTIVE | P1 | Background agents can't write files | All artifact-writing must be foreground. Background = read-only research only. **Recurrence**: LL-12 documents an explicit recurrence after LL-02 was already in the log — passive documentation does not prevent it. **Integration hook**: Pre-delegation gate that inspects `run_in_background: true` + intended tool list and blocks if Write/Edit/NotebookEdit are present. Currently held only by CLAUDE.md narrative. |
| LL-08 | 2026-04-04 | ops | ACTIVE | P1 | Background agent outputs must persist as artifacts | Task notification output files on disk may be empty. Any substantive agent analysis must write to `notes/`. Don't rely on temp output files. **Integration hook**: Post-agent verification step that checks for expected artifact files before treating agent output as authoritative. Currently held by `execute-task` skill convention only. |
| LL-12 | 2026-04-05 | ops | ACTIVE | P1 | Reinforce LL-02: never delegate file writes to background agents | Cross-ref update agent launched in background couldn't edit files — wasted tokens and caused context overflow requiring `/compact`. LL-02 was documented from day 1 but not consistently enforced. Lesson: treat LL entries as hard constraints, not suggestions. Re-read `lessons-learned.md` before any agent delegation decision. **Status**: Reinforces LL-02; both share the same integration hook. Recurrence pattern (LL-02 → LL-12 → LL-17 → LL-20) is the canonical evidence that "documented but not woven" is not enough. |
| LL-17 | 2026-04-08 | ops | ACTIVE | P1 | Concurrent CC sessions on same repo can cause cross-branch contamination | When multiple CC sessions operate on the same git repo (e.g., user on `main` + remediation agent on feat branch), subagent processes may checkout different branches and reset the working tree. Phase A commit landed on `main` instead of `feat/` branch because a parallel HydroCast session was active on `main`. **Mitigation**: (1) Use `git worktree` for parallel agent work on separate branches, (2) always verify `git branch --show-current` before committing, (3) consider branch-lock conventions. This is a CC CLI limitation — multiple sessions share the same `.git` lock. **Integration hook**: Pre-commit verification of branch context + auto-suggest worktree when concurrent-session risk is detected. KB exists in `multi-agent/worktree-workflows.md`; programmatic surface does not. |

### ACTIVE — P2 (nice to have)

| ID | Date | Cat | Class | Pri | Lesson | Detail |
|----|------|-----|-------|-----|--------|--------|
| LL-10 | 2026-04-05 | proc | ACTIVE | P2 | Fresh-fetch methodology validated | Files written from freshly-fetched official docs (same session) had 0 errors. Files modified across sessions without re-fetching had 5-8 errors each. For any KB update: always re-fetch source URL in the same session. Enum-type content (field values, mode lists, event counts) drifts fastest — prioritize in refresh cycles. **Integration hook**: Pre-edit gate on KB files referencing official CC docs that prompts (or hard-checks) for an in-session fetch via Context7 or WebFetch. Currently held by `claude-docs-helper` skill convention only. |
| LL-18 | 2026-04-08 | proc | ACTIVE | P2 | Audit skill must validate structural correctness, not just field presence | D1-2 remediation produced a settings.json with wrong `$schema` URL and invalid hooks structure — CC rejected the ENTIRE file. The audit skill correctly identified missing hooks/schema but didn't validate the STRUCTURE of what was written. Two gaps: (1) settings pack should validate against CC JSON schema, (2) remediation output should have a validation gate before commit. "Checking for missing fields" ≠ "validating structural correctness." **Status**: Mostly addressed by LL-22's Phase 1.5 gate for settings.json. Residual gaps in non-settings audit dimensions (component schemas, plugin manifest). |

### ADVISORY (heuristic — periodic review only)

| ID | Date | Cat | Class | Pri | Lesson | Detail |
|----|------|-----|-------|-----|--------|--------|
| LL-01 | 2026-04-03 | ops | ADVISORY | — | Explore agents sandboxed to current project dir | Use general-purpose agents for cross-project reads. Operational tip; no enforcement gate needed. |
| LL-03 | 2026-04-03 | ctx | ADVISORY | — | Post-compaction drops extension awareness | Re-read `notes/global-extensions-map.md` and restate objective after any `/compact`. Largely superseded by LL-25 tracked-notes-by-default + Session 32 cold-start protocol — re-read happens at session start now, not mid-session. Retained as advisory for the post-`/compact` re-anchor pattern. |
| LL-05 | 2026-04-03 | ops | ADVISORY | — | Worktree agents don't auto-commit | Orchestrator must manually handle commits when using worktree isolation. Operational tip captured in `worktree-workflows.md` KB card. |
| LL-06 | 2026-04-04 | ctx | ADVISORY | — | Fan-out parallel agents for doc-heavy research | Main session retains synthesis authority; subagents handle context-heavy fetching (~370K tokens across 8 agents, zero main-context impact). "Fan-out → synthesize" pattern. Delegation heuristic, not an enforcement target. |
| LL-09 | 2026-04-05 | proc | ADVISORY | — | Cross-check research agent output against source | Research agents can miss items from interactive/dynamic doc pages. Always verify summarized findings against the actual source (screenshot, page fetch, or manual check). Schema alignment tasks require exhaustive comparison, not spot-checking. General practice; partially overlaps with LL-10 (P2 ACTIVE). |
| LL-11 | 2026-04-05 | arch | ADVISORY | — | Wrapper files age better than comprehensive mirrors | `distributable-plugin.md` (wrapper philosophy, 2 errors) vs `global-user-config.md` (comprehensive mirror, 8 errors). Files that defer to official docs and only document CAB-specific patterns accumulate fewer errors over time. KB authoring philosophy — already canonized in KB authoring conventions (`KB files: ≤300 lines each, source: metadata required, wrapper philosophy over comprehensive mirroring`). |

### ARCHIVED (superseded / historical)

| ID | Date | Cat | Class | Pri | Lesson | Resolution |
|----|------|-----|-------|-----|--------|------------|
| LL-07 | 2026-04-04 | proc | ARCHIVED | — | Three-tier state hierarchy | (1) Implementation plan → (2) TODO.md (never delete, reorder pending to top) → (3) progress.md (live state, can compact). Only progress.md warrants deletions. **Superseded by**: LL-25 (multi-archetype `notes/` policy — all state files first-class, no compaction-by-default) + Session 32 flat `notes/` policy (no compaction tier hierarchy, just curation discipline + `_archive/` escape hatch). The "compactable progress.md" framing is incompatible with curation > compression. |

---

## Conventions (cross-cutting)

### LL → CLAUDE.md Promotion (manual, judgment-based)

**Context** (UXL-023 disposition, 2026-04-24): the original "Dream consolidation skill" concept (mimicking CC Layer 6 — Dreaming) was scoped to automate LL→CLAUDE.md promotion. After the auto-memory layer's introduction, that scope is **superseded for general memory consolidation** (auto-memory already decides what's worth remembering and writes to `MEMORY.md`). What remains is the narrow case of promoting durable cross-project learnings from `lessons-learned.md` into project or global `CLAUDE.md` — and this is a **judgment call best kept manual**, not a skill.

**Convention** — promote an LL entry to a CLAUDE.md when ALL of:

1. The lesson is **structural** (governs how work is done across projects), not procedural (one-time fix recipe)
2. It applies to **3+ projects** or to the global orchestrator persona
3. Forgetting it has caused recurrence at least once (empirical signal)
4. Promotion costs <10 lines of CLAUDE.md (otherwise: keep as LL reference and link from CLAUDE.md instead of inlining)

**Mechanism**: edit the relevant CLAUDE.md `## Learned Corrections` section directly; cross-link by citing `LL-NN` so the durable lesson stays single-source-of-truth in `lessons-learned.md`.

**Why not a skill**: frequency is too low (~1-2/quarter at most); judgment is the load-bearing input, not procedure; auto-memory + manual editing already cover the actual workflow without a dedicated skill's overhead. Documented per dual-POV check (`memory/feedback_dual_pov_check.md`).

---

## Pending

User has additional lessons-learned to add after audit cycles. New entries append at the bottom of the appropriate Class/Pri section.
