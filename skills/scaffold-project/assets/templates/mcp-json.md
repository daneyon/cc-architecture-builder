# .mcp.json Template

```json
{
  "mcpServers": {}
}
```

**Notes**:
- Empty scaffold — populate with MCP servers as needed
- For MCP server development patterns + integration:
  `knowledge/components/mcp-integration.md`
- For wrap-don't-rewrite philosophy when integrating external tools:
  `knowledge/overview/design-principles.md` (DP8)
