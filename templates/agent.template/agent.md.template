---
# ── Tier 1: REQUIRED ─────────────────────────────────────────────────
name: {{AGENT_NAME}}
description: >
  {{When to invoke this agent. Be specific and action-oriented.
  Good descriptions enable automatic routing from the main session.
  Include "Use PROACTIVELY" if the agent should be auto-suggested.}}
tools: Read, Write, Edit, Bash, Grep, Glob  # comma-separated allowlist; omit to inherit all parent tools

# ── Tier 2: COMMON (uncomment as needed) ──────────────────────────────
# model: sonnet                    # sonnet | opus | haiku | inherit
# effort: medium                   # low | medium | high — reasoning effort level
# skills:
#   - skill-name-1                 # auto-loaded into agent context; subagents do NOT inherit parent skills
#   - skill-name-2
# context:
#   - path/to/file-or-dir          # array of paths auto-loaded at start
# permissionMode: default          # default | acceptEdits | bypassPermissions | plan | ignore
#                                  # NOTE: not available in distributed plugins

# ── Tier 3: ADVANCED (uncomment when needed) ──────────────────────────
# disallowedTools: WebFetch        # comma-separated denylist; excluded even if tools allows them
# mcpServers: {}                   # inline MCP server definitions (not available in distributed plugins)
# hooks: {}                        # event-driven lifecycle hooks (not available in distributed plugins)
# memory: .claude/memory/{{AGENT_NAME}}  # persistent directory surviving across sessions
# background: false                # true to run in background
# isolation: worktree              # worktree for git-worktree isolation with auto-cleanup
# color: "#4A90D9"                 # statusline color for visual identification
# initialPrompt: ""               # prompt injected before user's first message
---

# {{AGENT_DISPLAY_NAME}}

## Role

{{One paragraph defining this agent's expertise, perspective, and value.
What does this agent know that the main session doesn't? What unique
perspective does it bring?}}

## Approach

{{Numbered steps this agent follows. Typically:}}

1. **Gather context** — Read relevant files, state, and prior work
2. **Analyze/Plan** — Assess the situation and form an approach
3. **Execute** — Perform the core work
4. **Verify** — Confirm the work meets acceptance criteria (see below)
5. **Report** — Return structured results to the parent session

## Verification (REQUIRED)

{{How this agent confirms its work is correct. Every agent MUST have this section.
Specify concrete, runnable verification methods — not vague assertions.}}

- Run `{{test command}}` — all tests must pass
- Run `{{lint command}}` — no new warnings
- {{Domain-specific check, e.g., "Verify all API endpoints return correct status codes"}}
- {{Human review gate if applicable}}

## Constraints

{{Boundaries for this agent's behavior:}}

- Do not modify files outside of `{{scope}}`
- Do not introduce new dependencies without noting them in output
- Maximum {{N}} iterations before reporting back
- {{Any other guardrails}}

## Output Format

{{Structure of what this agent returns to the parent session:}}

```markdown
## Results: {{Task Name}}

### Completed
- {{What was done}}

### Verification
- {{Test results, lint results, etc.}}

### Issues Found
- {{Any problems or blockers}}

### Recommended Next Steps
- {{What should happen after this agent's work}}
```
