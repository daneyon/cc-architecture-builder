---
# ── Tier 1: REQUIRED ─────────────────────────────────────────────────
name: {{SKILL_NAME}}
description: {{SKILL_DESCRIPTION}}. Use when {{TRIGGER_CONDITIONS}}.

# ── Tier 2: COMMON (uncomment as needed) ─────────────────────────────
# argument-hint: {{ARGUMENT_HINT}}
# allowed-tools: {{ALLOWED_TOOLS}}         # tools available without permission prompts
# effort: {{EFFORT_LEVEL}}                 # low | medium | high | max
# model: {{MODEL_OVERRIDE}}               # e.g., claude-sonnet-4-20250514

# ── Tier 3: ADVANCED (uncomment as needed) ───────────────────────────
# disable-model-invocation: false
# user-invocable: true
# context: fork                     # omit for default inline context; "fork" = isolated subagent
# agent: Explore                    # subagent TYPE when context: fork (string, NOT boolean)
# hooks: {}                         # skill-scoped lifecycle hooks
# shell: {{SHELL_OVERRIDE}}
#
# Invocation Control Matrix:
#   disable-model-invocation | user-invocable | Who can invoke
#   false                    | true           | Model + User (default)
#   true                     | true           | User only
#   false                    | false          | Model only
#   true                     | false           | Nobody (disabled)
#
# Substitution Variables:
#   $ARGUMENTS — full argument string; $ARGUMENTS[0], $ARGUMENTS[1] — 0-indexed
#   $0, $1, $2 — shorthand for $ARGUMENTS[N]
#   ${CLAUDE_SKILL_DIR} — absolute path to this skill's directory
---

# {{SKILL_TITLE}}

> **Scaffolding note**: For basic skill creation, use the built-in `skill-creator` plugin
> (`/skill-creator`). This CAB template extends beyond basics with orchestration integration,
> verification requirements, and state management patterns.

## Purpose & Pipeline Position

{{BRIEF_OVERVIEW}}

**Pipeline position**: {{PIPELINE_POSITION}} (e.g., FIRST, MIDDLE, LAST, STANDALONE)
**Depends on**: {{UPSTREAM_SKILLS_OR_NONE}}
**Feeds into**: {{DOWNSTREAM_SKILLS_OR_NONE}}

## Orchestration Integration

How this skill integrates with the multi-agent orchestration framework:

- **Delegation routing**: {{WHEN_ORCHESTRATOR_DELEGATES_HERE}}
- **Skill-first rule**: This skill MUST be invoked instead of manual implementation for: {{ANTI_PATTERNS}}
- **State interaction**: {{READS_FROM_OR_WRITES_TO_STATE_FILES}}

## Instructions

1. {{STEP_1}}
2. {{STEP_2}}
3. {{STEP_3}}
4. {{STEP_4}}
5. {{STEP_5}}

## Verification

How to verify this skill's output is correct:

```bash
{{VERIFICATION_COMMAND}}
```

- {{VERIFICATION_CHECK_1}}
- {{VERIFICATION_CHECK_2}}

## When to Apply

- {{TRIGGER_1}}
- {{TRIGGER_2}}
- {{TRIGGER_3}}

## Output Format

{{OUTPUT_DESCRIPTION}}

```
{{OUTPUT_TEMPLATE}}
```

## References

- `knowledge/components/agent-skills.md` — CAB skill field spec and design patterns
- {{DOMAIN_REFERENCE_1}}
- {{DOMAIN_REFERENCE_2}}
