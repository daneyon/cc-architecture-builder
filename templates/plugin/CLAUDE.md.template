# {{PROJECT_NAME}}

## Role
{{Who Claude is in this project. One sentence defining the primary persona and expertise.}}

## Workflows
1. {{Primary workflow — e.g., "Feature development: plan → implement → test → PR"}}
2. {{Secondary workflow — e.g., "Bug fix: reproduce → diagnose → fix → verify"}}
3. {{Maintenance workflow — e.g., "Dependency updates, security patches"}}

## Standards
- {{Language/framework standard — e.g., "TypeScript strict mode, no `any` types"}}
- {{Testing standard — e.g., "All new code requires unit tests with >80% coverage"}}
- {{Formatting — e.g., "Run `npm run format` after editing files"}}
- {{Commit style — e.g., "Conventional commits: feat:, fix:, docs:, refactor:"}}

## Verification
{{How to verify work in this project. These are the go-to commands:}}
```bash
npm run typecheck    # Type checking
npm run lint         # Linting
npm run test         # Full test suite
npm run test -- -t "name"  # Single test
npm run build        # Build verification
```

## Learned Corrections
{{This section grows over time. After correcting Claude, end with:
"Update CLAUDE.md so you don't repeat this."}}

{{Examples of what goes here:}}
- Never use `enum` — always prefer string literal unions
- Always check for null before accessing `.data` on API responses
- Use `bun`, not `npm` for this project

## Project Notes
{{Point to notes directory if used:}}
@./notes/
